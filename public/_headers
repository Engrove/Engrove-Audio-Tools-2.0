# public/_headers

# Regel för datafiler. Tvinga omvalidering.
/data/*
  Content-Type: application/json
  Cache-Control: public, max-age=0, must-revalidate

# NY REGEL för byggda assets (JS/CSS).
# Instruera webbläsaren att cacha dessa filer under lång tid (ett år),
# eftersom de har unika hash-namn. Detta är en standard "best practice".
# Det viktiga är att detta signalerar till Cloudflare att dessa filer är
# "immutable" (oföränderliga), vilket kan påverka hur deras cache hanteras.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# public/_headers
